pragma solidity 0.8.19;

interface IDzap {
    // event BridgeTransferStarted(
    //     bytes32 transactionId,
    //     address indexed integrator,
    //     address indexed sender,
    //     address refundee,
    //     BridgeData bridgeData
    // );
    // event RubicTransferStarted(IRubic.BridgeData bridgeData);
    // event RubicTransferCompleted(bytes32 indexed transactionId, address receivingAssetId, address receiver, uint256 amount, uint256 timestamp);
    // event RubicTransferRecovered(bytes32 indexed transactionId, address receivingAssetId, address receiver, uint256 amount, uint256 timestamp);
}
